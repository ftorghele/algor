%h2 Aufgabe 1.1

- ["0316095648", "0971880107", "0446610038"].each do |isbn|
  .well.well-small
    %p= "Buch #{isbn}:"
    %ul
      %li= rating_avg(isbn)
      %li= rating_median(isbn)
      %li= rating_p_quantil(isbn,0.25)

%h2 Aufgabe 1.2

- ["1903", "2033", "2766"].each do |user_id|
  .well.well-small
    %p= "User #{user_id}:"
    %ul
      %li= user_rating_avg(user_id)
      %li= user_rating_sx(user_id)

%h2 Aufgabe 1.3

- related_users(276688).each do |user_id|
  .well.well-small
    %p= "User 276688 vs. User #{user_id}"
    %ul
      = compare_users(276688, user_id)
